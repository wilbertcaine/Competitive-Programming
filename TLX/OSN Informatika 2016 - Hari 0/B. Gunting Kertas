#include <bits/stdc++.h>

using namespace std;

long long n, m;

string s;

int main()
{

    cin >> s;

    for (long long i = 0; i <= 100000; i++)
    {

        cin >> n >> m;

        if (n < m)

            cout << n << ' ' << n << "\n";

        else

            cout << m << ' ' << m << "\n";

        if ((n == 1) || (m == 1))

            break;
    }
}
